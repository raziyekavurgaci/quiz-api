import {
  IsString,
  IsArray,
  ValidateNested,
  IsEnum,
  IsBoolean,
  ArrayMinSize,
  ArrayMaxSize,
} from 'class-validator';
import { Type } from 'class-transformer';
import { OptionLabel } from '@prisma/client';
